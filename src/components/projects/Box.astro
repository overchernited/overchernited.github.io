---
import { cn } from "../../utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn("rounded-3xl border border-white/10 bg-white/[0.03] p-8 backdrop-blur-sm md:p-12", className)}>
  <slot />
</div>