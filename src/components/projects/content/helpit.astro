---
import Title from "../Title.astro";
import Box from "../Box.astro";
import { useTranslations, type Locale } from "../../../i18n";

export interface Props {
  locale?: string;
}

const { locale: rawLocale } = Astro.props;
const locale = (rawLocale as Locale) || "en";
const _ = useTranslations(locale);
const t = (key: string) => _(`projects.content.helpit.${key}`);
---

<section class="flex flex-col gap-8">
  <div>
    <Title>{t("what")}</Title>
    <p class="mt-4 text-lg leading-8 text-slate-300">
      Helpit es una red social enfocada en salud mental...
    </p>
  </div>

  <div>
    <Title>{t("problem")}</Title>
    <p class="mt-4 text-lg leading-8 text-slate-300">
      Muchas personas...
    </p>
  </div>

  <div>
    <Title>{t("solution")}</Title>
    <p class="mt-4 text-lg leading-8 text-slate-300">
      Creamos una plataforma...
    </p>
  </div>

  <div>
    <Title>{t("demo")}</Title>
    <p class="mt-4 text-lg leading-8 text-slate-300">
      Demo content...
    </p>
  </div>

  <Box>
    <p class="text-lg leading-8 text-slate-300">Hola asdasd</p>
  </Box>
</section>
